# SDK and binding tests

Verify that generated APIs and native calls preserve types, values, errors, callbacks and resource ownership. Link engine behavior to its existing tests; exercise it here when crossing the boundary introduces a distinct failure mode.

Existing entry points are [common binding tests](../../../../bindings/common/tests), [JavaScript tests](../../../../integration/bindings/client-js), [Dart tests](../../../../packages/dart/test) and [generated API fixtures](../../../../integration/generated-api).

After the prerequisites and native build in [Running tests](../running.md):

```sh
node --test integration/bindings/client-js/*.test.mjs
node --test integration/bindings/client-react-native/*.test.mjs
bash integration/generated-api/verify.sh
```

The generated API runner checks TypeScript positive and negative cases, analyzes Dart, and executes generated clients. Dart native tests also need the library-path environment described in the running guide.

Next review: negative type coverage per language, callback failures, native lifetimes and shared cross-language scenarios. A shared Rust engine alone does not establish SDK equivalence.

## React Native checks

[Mobile adapter tests](../../../../integration/bindings/client-react-native) exercise scope lifetime, rollback/isolation on real SQLite through the host carrier, and native-style HTTP/WebSocket authentication, subscription ordering, cancellation and bounded recovery. [C carrier tests](../../../../bindings/mobile/src/lib.rs) exercise malformed input and handle lifetime. These host checks do not establish iOS execution. The separate [simulator harness](../../../../integration/platform/react-native/README.md) owns actual Expo/Swift/Rust, offline process-restart and two-client evidence.

## Coverage review

Reviewed 2026-09-14; tests read, not executed, except where a row records a verified run. Verified 2026-09-14: `bash integration/persistence/transaction-probe/run.sh` passed (12 tests) over the probe build.

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Command contract: session isolation, closed handles, transport actions, lanes, dispositions ([Bindings](../../architecture/sdks/bindings.md)) | [session.rs](../../../../bindings/common/tests/session.rs); `transaction_scoped_commands_and_sync_commands_are_refused_by_code` (a transaction-scoped read before and after a session is `transaction_closed`; `freeze`, `status`, `tasks`, `task` and `outcome` during a session are `client transaction active`; a closed handle is `client_closed`) | covered | none |
| Transaction semantics across the boundary: unawaited calls, caught failures, overlapping and unawaited savepoints, late callbacks ([Typed API / Client](../../architecture/sdks/typed-api/client.md)) | [transaction.test.mjs](../../../../integration/bindings/client-js/transaction.test.mjs); Dart [client_test.dart](../../../../packages/dart/test/client_test.dart) group `Dart callbacks through native Rust`: one test per clause (own writes and rollback on throw, unawaited call, caught failure, savepoint scope, late child savepoint, reopen and closed handle), each over a fresh file | covered in both languages | none |
| Async host callbacks inside a Prisma transaction; rollback on Rust error, dispose, timeout, concurrent transactions | [transaction-bridge.test.mjs](../../../../integration/bindings/node/transaction-bridge.test.mjs) | covered for the probe build | These tests drive `runProbe` through the fixture [transaction-session.mjs](../../../../integration/bindings/node/transaction-session.mjs) over `axton-node-probe.node`, built by `node bindings/node/build.mjs --probe` in [transaction-probe/run.sh](../../../../integration/persistence/transaction-probe/run.sh). The normal addon has no probe API. Unique to the probe: dispose during an awaited callback, transaction timeout, a synchronous callback throw, and a deferred-constraint commit failure. Already proven on the production path by [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs): rollback on a broken transaction (`a handler that breaks the transaction still fails the delivery`; a thrown handler or loader error is isolated to its own mutation instead, see [Server Push §9](../../architecture/server/engine/push.md#9-architecture-decisions)), unawaited work (`pending unawaited publication prevents outer transaction commit`), caught failures (`external transaction binding retains swallowed publication failure…`), concurrent transactions (`repeatable-read runner keeps head, scan, and loader coherent…`). Do not cite the probe tests for `createBackend` behavior. |
| Generated TypeScript: positives run against native Rust, negatives fail to compile | [test.ts](../../../../integration/generated-api/test.ts), [native.mts](../../../../integration/generated-api/native.mts), [verify.sh](../../../../integration/generated-api/verify.sh) | covered | The negatives include a bare function and a `v3` key on the two-version `EditEntry` handler registration. Verified 2026-09-15 by `bash integration/generated-api/verify.sh`. |
| Generated Dart: positives run against native Rust; a failed open closes the isolate; misuse is refused by the analyzer | [generated_test.dart](../../../../integration/generated-api/generated_test.dart), [failed_open.dart](../../../../integration/generated-api/failed_open.dart); [negative/misuse.dart](../../../../integration/generated-api/negative/misuse.dart) checked by [negative/check.sh](../../../../integration/generated-api/negative/check.sh) from `verify.sh` (a list as a filter or patch field, ordering by an enum, a string where a `DateTime` is required, `watch` inside a transaction, an identity field in a patch, `null` for a non-nullable field, an undeclared enum value: every case must be reported, and a fixture that analyzes cleanly fails the check) | covered | The negative directory has its own analysis context so `dart analyze integration/generated-api` stays clean. Verified 2026-09-14 by `bash integration/generated-api/verify.sh`. |
| Dart requires `libraryPath` outside iOS; close is idempotent and waits for connect | [client_test.dart](../../../../packages/dart/test/client_test.dart) | covered | none |
| `runPrerequisites` in Dart | [prerequisite_test.dart](../../../../packages/dart/test/prerequisite_test.dart) `prerequisite failure stays optimistic and explicit retry unlocks the push` (the Dart equivalent of [prerequisite.test.mjs](../../../../integration/bindings/client-js/prerequisite.test.mjs), plus: no handler needed when nothing is pending; a run without the handler fails the task with `missing prerequisite handler` and a later run with it succeeds) | covered | none |
| Identical state across TypeScript and Dart for one script | [parity.test.mjs](../../../../integration/e2e/parity.test.mjs) with [parity_client.dart](../../../../integration/e2e/parity_client.dart) under [End-to-end](../end-to-end.md) | covered (TypeScript and Dart) | A Rust-native runner is deliberately not built: the engine and the live session are shared, and their rules are the Rust suites' job; the parity script checks the host-side executors and value handling, which are per language ([Live session](../../architecture/client/connection/controller/live-session.md)). [fixtures/scenarios](../../../../fixtures/scenarios) remain prose. |
| Server errors cross as `{code, message, details?}`; classification never depends on wording ([Bindings](../../architecture/sdks/bindings.md)) | [server/tests/runtime.rs](../../../../crates/server/tests/runtime.rs) `push_refusals_carry_stable_codes_and_run_no_handler`, `malformed_requests_and_blank_owners_are_refused_with_codes`, `host_failures_keep_their_message_under_the_host_code`; [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs) `HTTP classifies native failures by code, not message wording; unknown codes fall back to 500` | covered | Client-direction errors remain messages by design (Bindings §11). |
