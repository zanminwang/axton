# Schema contract tests

Verify what a schema describes: identities, field types, relationships, mutations and valid combinations. The [schema documents](../../architecture/schema/README.md) define these rules; compiler tests verify how source declarations become that contract.

Existing entry points: [core contracts](../../../../crates/core/tests/contracts.rs) and [compiler tests](../../../../crates/compiler/tests).

```sh
cargo test -p axton-core --test contracts --locked
cargo test -p axton-compiler --locked
```

Assert accepted and rejected descriptors, field presence and nullability, identity constraints and relationship rules. Keep received-state validation distinct from backend loader validation: their treatment of unknown fields differs. Database reconciliation belongs in [storage integration](../integration/persistence.md).

## Coverage review

Reviewed 2026-09-14 by reading test assertions against the schema documents; no tests were executed for this review. Coverage terms: *covered* means an assertion checks the clause; *partial* means only some clauses or only an indirect path; *missing* means no assertion was found.

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Integers outside ±2^53−1 are refused; UUIDs normalize to lowercase and must be RFC 4122 ([Types](../../architecture/schema/types.md)) | [contracts.rs](../../../../crates/core/tests/contracts.rs) `state_is_complete_but_patch_preserves_absent_and_null`, `identities_are_exact_normalized_and_independent_of_channels` | covered | none |
| `dateTime` re-encodes to UTC milliseconds; `float` must be finite and `-0` becomes `0`; enum values must be declared | [contracts.rs](../../../../crates/core/tests/contracts.rs) `scalar_and_enum_values_normalize_or_are_refused` (offset and fractional dateTimes re-encode to UTC milliseconds; a date, a space separator and a number are refused; `-0` becomes `0`, strings are refused; undeclared and non-string enum values refused; list elements normalized, non-lists and bad elements refused); Dart [generated_test.dart](../../../../integration/generated-api/generated_test.dart) filters `at` with a `+01:00` offset through the native client | covered | JSON cannot carry NaN or infinity, so those are refused only as non-numbers. |
| A list element must be a scalar and a list cannot be nullable; an enum type must be declared | `list_descriptors_must_hold_scalars_and_cannot_be_nullable` | covered | none |
| A push batch holds 1–20 mutations with distinct positive ordinals ([Protocol / Push](../../architecture/protocol/push.md)) | `push_batches_hold_one_to_twenty_mutations_with_distinct_ordinals` | covered | none |
| Identity must be one or more non-nullable scalar fields; `axton_` and `sqlite_` prefixes refused in any case; names unique ([Models](../../architecture/schema/models.md)) | [compiler.rs](../../../../crates/compiler/tests/compiler.rs) `rejects_invalid_identity`, `schema_and_mutations`, `rejects_reserved_model_names_at_the_declaration`, `semantic_errors_report_the_offending_declaration` (duplicate model, missing identity); [contracts.rs](../../../../crates/core/tests/contracts.rs) `field_default_and_record_stamp_round_trip_and_axton_prefix_is_rejected` (`sqlite_x`, `SQLITE_x`, `AXTON_x` refused; `sqlitex`, `Sqlite`, `axtonx` accepted) | covered | Duplicate field names: `structural_refusals_a_schema_author_is_likely_to_hit` in [compiler.rs](../../../../crates/compiler/tests/compiler.rs). |
| `@@unique` is enforced atomically on the client (L3) | [client.rs](../../../../crates/sqlite/tests/client.rs) `declared_unique_constraint_is_atomic` | covered | none |
| Reference `via` arity and field types must match the target identity; `onTargetDelete` is `delete` or `none`; no other reference argument ([Relations](../../architecture/schema/relations.md)) | positive case in `relationships_bindings_and_dependency_metadata`; unknown `via` field and unknown `onTargetDelete` in `semantic_errors_report_the_offending_declaration`; arity mismatch, type mismatch and an unknown argument in `structural_refusals_a_schema_author_is_likely_to_hit` (each with a valid twin that differs only in that rule) | covered | none |
| Inverse resolution requires exactly one reference; singular inverse needs a unique key | `singular_inverse_requires_a_unique_foreign_key`; ambiguous inverse (two references to the parent without a shared relation name) and its named-relation twin in `structural_refusals_a_schema_author_is_likely_to_hit` | covered | none |
| Cascades apply on direct writes, queued mutations and authoritative deletes (L5) | client, downlink and sim tests listed under [Client tests](client.md) | covered | none |
| Slot decoding: order by `(model, op)`, list slots greedy, stable rejection codes ([Mutations](../../architecture/schema/mutations.md)) | [server/tests/runtime.rs](../../../../crates/server/tests/runtime.rs) | covered for single slots | Adjacent slots with the same model and operation are refused by the compiler ([Mutations §9](../../architecture/schema/mutations.md#9-architecture-decisions)); the fixture's `RemoveEntries` v2 is unambiguous. An empty patch is a no-op update, covered by `empty_patch_decodes_as_a_no_op_update` ([Mutations §9](../../architecture/schema/mutations.md#9-architecture-decisions)). |
| Version history: no decrease, no removal, compatible changes accepted | [history.rs](../../../../crates/compiler/tests/history.rs), [cli.rs](../../../../crates/compiler/tests/cli.rs) | covered | Enum-value growth and the "create slot gains a non-nullable field" rule are implemented but not asserted individually. |
| Prerequisite keys derive from operation values; failed tasks block until reset ([Prerequisites](../../architecture/schema/prerequisites.md)) | [push.rs](../../../../crates/sqlite/tests/push.rs) `schema_requirements_create_durable_tasks_and_gate_only_dependent_mutation`, `failed_prerequisite_stays_optimistic_independent_work_can_overtake`, `late_task_completion_does_not_resurrect_unused_readiness`; [prerequisite.test.mjs](../../../../integration/bindings/client-js/prerequisite.test.mjs) | covered (both runners) | The Dart runner is covered by [prerequisite_test.dart](../../../../packages/dart/test/prerequisite_test.dart); see [SDK and binding tests](../integration/bindings.md). |
