# Client tests

Verify local transactions, dependency eligibility, stable frozen batches, page application and completion from receipts. See [Client architecture](../../architecture/client/README.md).

Many engine tests currently live in [sqlite/tests](../../../../crates/sqlite/tests), where real SQLite provides the client harness. Retry scheduling also has inline tests in [connection.rs](../../../../crates/client/src/connection.rs).

```sh
cargo test -p axton-client --locked
cargo test -p axton-sqlite --locked
```

For a batching change, assert which mutations are eligible and whether retry bytes remain stable. For settlement, assert the resulting visible state, the before image and the pending work, rather than only the pending count; a receipt and a page for the same change must leave the same state in either order.

Use [Simulation](../simulation/README.md) for interactions across clients and server. Next review: identify missing assertions and distinguish engine coverage from SQLite-specific coverage.

## Coverage review

Reviewed 2026-09-14 against the client architecture documents; tests read, not executed. Pull, settlement and frontend rows rewritten 2026-09-15 for completion from receipts and record retention ([#55](https://github.com/zanminwang/axton/issues/55)); executed 2026-09-15: `cargo test -p axton-core -p axton-server -p axton-client -p axton-sqlite -p axton-binding --locked` passed, which includes [settlement.rs](../../../../crates/sqlite/tests/settlement.rs), [downlink.rs](../../../../crates/sqlite/tests/downlink.rs) and [ddl.rs](../../../../crates/sqlite/tests/ddl.rs). Tests in [sqlite/tests](../../../../crates/sqlite/tests) use real SQLite as the harness but assert engine rules, so they are counted here as component evidence; the SQLite contract itself is under [Storage and persistence](../integration/persistence.md). Simulation scenarios that establish the same clause across client and server are listed under [Simulation scenarios](../simulation/scenarios.md) and referenced here only where they add a clause.

### Local operations

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| A write is readable at once; the before image is taken once; dropping the last pending mutation restores it ([Writes](../../architecture/client/engine/local-operations/writes.md)) | [client.rs](../../../../crates/sqlite/tests/client.rs) `optimistic_edit_holds_truth_once_and_rejection_rebuilds_from_it`, `session_reads_own_writes_without_notifying_until_commit_and_blocks_other_writes` | covered | none |
| A failed step rolls back the transaction; a savepoint confines its scope (L3) | `local_transaction_and_mutation_savepoint_have_independent_fate`, `declared_unique_constraint_is_atomic` | covered | none |
| Direct writes never queue; a direct write on a dirty authoritative record survives rejection (L4) | `l4_…` in [sim local.rs](../../../../crates/sim/tests/local.rs); [push.rs](../../../../crates/sqlite/tests/push.rs) `rejection_removes_optimism_preserves_direct_truth_and_has_durable_inbox` | covered | The pending-create case ([#33](https://github.com/zanminwang/axton/issues/33)) is fixed and covered by `l4_direct_write_on_pending_create_goes_with_the_rejected_create` and the no-longer-ignored `random_sequences_with_direct_writes`; see [Simulation scenarios](../simulation/scenarios.md). |
| Cascades apply to descendants once, including cycles, on all three paths; an authoritative cascade does not rewrite the descendants' stamps (L5, D5) | `schema_cascade_is_optimistic_same_fate_and_not_extra_wire_operations`, `direct_cascade_handles_cyclic_relationships_once`; [downlink.rs](../../../../crates/sqlite/tests/downlink.rs) `delete_cascades_to_descendants_and_keeps_their_stamps` | covered | none |
| Server truth lands in the before image and pending edits replay on top (A1) | `newer_authority_lands_beneath_pending_edits_and_replays_them`; `pull_before_ack_and_later_local_edit_replay_in_order` | covered | none |
| A replay that no longer applies falls back to the before image silently | none | missing | Behavior is a *potential risk* in Writes §11; decide whether to surface it before writing an expectation. |
| Queries: equality filters, null matching, ordering with nulls first and identity tiebreak, limit, relation navigation ([Queries](../../architecture/client/engine/local-operations/queries.md)) | [query.rs](../../../../crates/sqlite/tests/query.rs) `query_normalizes_filters_orders_nulls_and_resolves_relationships`; refusals in `unsupported_filter_and_order_shapes_are_refused` (list predicate, unknown filter field, undeclared enum value, ordering by an enum or list field, unknown order field) | covered | none |
| Read-only SQL sees optimistic rows and refuses writes | `readonly_sql_sees_optimistic_rows_and_refuses_write_statements` | covered | none |

### Push

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Queue rows reconstruct mutations, operation kinds and dependencies ([Queue](../../architecture/client/engine/push/queue.md)) | [engine.rs](../../../../crates/sqlite/tests/engine.rs) `queue_rows_reconstruct_mutations_and_cascade_on_delete` | covered | Counter exhaustion at 2^53−1 returns an error by code; not asserted (low value). |
| Lifecycle dependents wait for the parent's receipt and never share its batch (P3) | [push.rs](../../../../crates/sqlite/tests/push.rs) `lifecycle_dependency_waits_for_parent_ack`; [sim push.rs](../../../../crates/sim/tests/push.rs) `p3_lifecycle_dependent_waits_for_the_parent_receipt`; [client.rs](../../../../crates/sqlite/tests/client.rs) `creating_then_editing_a_record_automatically_has_lifecycle_dependency` | covered | none (the SQLite test was renamed to what it asserts). |
| Sequence dependents may share the batch with their predecessor ([Dependencies](../../architecture/client/engine/push/dependencies.md)) | `schema_sequence_relationship_freezes_dependent_with_its_predecessor` (two mutations frozen together after readiness) | covered | none (renamed to what it asserts). |
| Unready or failed prerequisites block only their mutation | `failed_prerequisite_stays_optimistic_independent_work_can_overtake`, `schema_requirements_create_durable_tasks_and_gate_only_dependent_mutation` | covered | none |
| An explicit dependency on an unknown ordinal is refused | `frozen_mutations_cannot_be_dropped_and_unknown_dependencies_are_refused` (lifecycle and sequence dependency on ordinal 99; the refused enqueue leaves no row) | covered | none |
| Frozen bytes, and the declaration frozen with them, are stable across retry and restart (P4) | `offline_queue_and_frozen_bytes_survive_restart_and_receipt_completes_at_once`; [settlement.rs](../../../../crates/sqlite/tests/settlement.rs) `frozen_batch_and_its_declaration_survive_restart_until_completed`; `p4_frozen_bytes_are_stable`; [session.rs](../../../../bindings/common/tests/session.rs) `rust_selects_transport_actions_and_reuses_frozen_request_on_retry` | covered | Bytes after a supported schema reconciliation with a populated queue: `populated_queue_survives_additive_reconciliation_with_frozen_bytes_unchanged` ([Storage and persistence](../integration/persistence.md)). |
| Byte budget: oversized candidates are skipped, the first eligible one is selected even if it exceeds a non-zero budget, zero budget yields nothing ([Batching](../../architecture/client/engine/push/batching.md)) | `byte_budget_skips_large_candidate_but_always_allows_one`; `batch_holds_at_most_twenty_mutations_and_zero_budget_freezes_nothing` (21 queued: zero budget freezes and assigns nothing, the first batch holds ordinals 1–20 and is returned unchanged until its receipt, the second holds ordinal 21) | covered | none |
| An in-flight batch is returned unchanged until its receipt | `p3_lifecycle_dependent_waits_for_the_parent_receipt` (retry is byte-identical) | covered | none |

### Pull

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| A page is applied whole: each named channel is gated, a gap on any channel applies nothing, a stale channel is a no-op, and every applied channel moves to its `to`; a record shared by two channels lands once (A2) | [downlink.rs](../../../../crates/sqlite/tests/downlink.rs) `a_page_moves_every_channel_it_names_and_a_shared_record_lands_once`, `channels_are_gated_one_by_one_and_a_gap_holds_the_whole_page`; [stamp_scenarios.rs](../../../../crates/sqlite/tests/stamp_scenarios.rs) `redelivered_page_is_a_no_op`; sim `a2_pages_apply_only_in_cursor_order`, `two_channels_sharing_a_record_arrive_in_one_pull` | covered | A page from a previous subscription of the same channel ([#32](https://github.com/zanminwang/axton/issues/32)) is fixed: [downlink.rs](../../../../crates/sqlite/tests/downlink.rs) `page_from_a_previous_subscription_is_stale_not_a_gap` covers `apply_page`, `receive_downlink` and `SyncCycle`, and the sim scenario is enabled. |
| Covered, applied and recover dispositions are the same for HTTP and streamed pages ([Pull](../../architecture/client/engine/pull.md)) | [session.rs](../../../../bindings/common/tests/session.rs) `incoming_pages_share_cursor_policy_and_do_not_overwrite_push_cycle`, `incoming_overlap_is_identical_with_or_without_http_request_metadata` | covered | none |
| Content applies by stamp: newer wins, equal idempotent or diagnostic, older discarded but the cursor advances; cursors and stamps are independent counters (D2, D3) | `older_stamp_cannot_regress_newer_authority_but_advances_the_cursor`, `equal_stamp_is_idempotent_or_a_diagnostic`, `cursor_and_stamp_are_independent`; [stamp_scenarios.rs](../../../../crates/sqlite/tests/stamp_scenarios.rs) `delayed_page_from_another_channel_cannot_regress_newer_content`, `redelivered_page_is_a_no_op` | covered | none |
| Deletes apply by stamp across channels, keep their stamp so stale content cannot resurrect the record, cascade locally without rewriting descendants' stamps, and survive reopen; a record moves between channels and back (D4, D5) | `cross_channel_delete_applies_by_stamp_and_retains_the_stamp`, `delete_cascades_to_descendants_and_keeps_their_stamps`; [stamp_scenarios.rs](../../../../crates/sqlite/tests/stamp_scenarios.rs) `delete_keeps_its_stamp_so_stale_content_cannot_resurrect_the_record`, `move_between_channels_and_back`, `reopen_preserves_stamps_and_tombstones` | covered | Child records moving with their parent are a simulation scenario ([Simulation scenarios](../simulation/scenarios.md)). |
| Unsubscribing deletes the subscription row only: records, stamps, before images and pending operations are retained, another channel still updates them, restart keeps them, and the cursor restarts at zero on resubscribe (D6) | `unsubscribing_retains_records_and_later_pages_are_dropped`, `another_channel_updates_retained_content_and_restart_keeps_it`; [client.rs](../../../../crates/sqlite/tests/client.rs) `unsubscribe_retains_records_and_restarts_from_zero` | covered | Retained content is never evicted ([#61](https://github.com/zanminwang/axton/issues/61)); no test asserts an eviction policy because none exists. |
| Nothing is silent (D7, D8): a change the schema refuses is skipped alone and reported, an `error` change keeps the local content and stamp and is reported as `readFailed`, a conflict and a divergence are reported; the cursor still advances; reports reach the SDK `onError` as `AxtonReport` ([Pull](../../architecture/client/engine/pull.md)) | `a_change_the_schema_refuses_is_reported_and_the_cursor_still_advances`, `an_error_change_keeps_local_content_and_stamp_and_is_reported`, `a_conflict_is_reported_through_receive_downlink`; [settlement.rs](../../../../crates/sqlite/tests/settlement.rs) `a_pending_update_over_a_deleted_base_diverges_and_is_still_sent`, `a_pending_create_over_an_existing_base_diverges`, `divergence_is_reported_from_a_receipt_too`; [downlink_worker.rs](../../../../crates/sqlite/tests/downlink_worker.rs) `reports_reach_the_host_as_actions`; sim [reports.rs](../../../../crates/sim/tests/reports.rs) | covered | Resolved [#51](https://github.com/zanminwang/axton/issues/51) and [#122](https://github.com/zanminwang/axton/issues/122). |
| A page for an unsubscribed channel is dropped whole; a page from a previous subscription of the channel is stale, not a gap, on every incoming path | `unsubscribing_retains_records_and_later_pages_are_dropped`, `page_from_a_previous_subscription_is_stale_not_a_gap`, `older_subscription_response_cannot_discard_a_fresh_response` | covered | none |
| The historical load's ledger (D10): registration before and after an origin, duplicate calls sharing a run, an explicit retry over the same progress, a page refused as stale, a protocol-invalid envelope, a record failure that keeps the page's successful authority and holds the interval, a bounded stored failure, a `Diverged` replay that does not fail the run, older historical authority that cannot regress newer content or land above pending optimism, the terminal page's fixed barrier and its settlement, an empty zero-head Scope, removal with the registration, and a failed commit that leaves neither authority nor progress ([Pull](../../architecture/client/engine/pull.md)) | [sqlite/tests/bootstrap.rs](../../../../crates/sqlite/tests/bootstrap.rs) (22 tests, real SQLite) | covered | Executed 2026-09-25: `cargo test -p axton-sqlite --test bootstrap --locked`. Crash coverage is close/reopen at commit boundaries, not arbitrary crash points (R3). |
| The historical load's scheduling (D10): no page before the origin is committed and the first page as soon as it is, one request in flight, rotation across Scopes, live delivery and a queued page continuing beside it, one commit per pump, a transport failure deferred and re-asked, a refusal that fails the run until an explicit retry, a stale or post-stop answer that writes nothing, socket replacement keeping the request, and a reopen that resumes or settles ([Downlink worker](../../architecture/client/connection/controller/downlink-worker.md)) | [sqlite/tests/bootstrap_worker.rs](../../../../crates/sqlite/tests/bootstrap_worker.rs) (20 tests, scripted host, real SQLite) | covered | Executed 2026-09-25: `cargo test -p axton-sqlite --test bootstrap_worker --locked`. Real sockets and HTTP stay under [Connection integration](../integration/connection.md); the public surface is in [Typed client](../../architecture/sdks/typed-api/client.md). |

### Settlement

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| A successful push completes from its receipt alone, with the server's content, with no subscription; the frozen bytes and declaration survive restart until completion; `record_status` reports `queued` and `frozen` only (A3) | [settlement.rs](../../../../crates/sqlite/tests/settlement.rs) `response_completes_without_a_subscription`, `frozen_batch_and_its_declaration_survive_restart_until_completed`; [push.rs](../../../../crates/sqlite/tests/push.rs) `offline_queue_and_frozen_bytes_survive_restart_and_receipt_completes_at_once`, `record_status_reports_phases_and_duplicate_ack_is_idempotent`; [query.rs](../../../../crates/sqlite/tests/query.rs) `transport_pulls_only_subscribed_channels_and_the_receipt_completes_the_push`; sim `a_push_completes_from_its_receipt_with_zero_subscriptions`, `a_change_published_to_no_channel_still_completes`, `batches_complete_on_their_receipts_without_any_page` | covered | The [#52](https://github.com/zanminwang/axton/issues/52) regressions (update reverting to its base, create disappearing) are gone with the behavior they pinned: the receipt now carries the server's row. |
| Staging order: authority lands beneath the pending operation before it is removed; a pending create survives with the server's content; clean extra authority is written and kept ([Settlement §9](../../architecture/client/engine/settlement.md#9-architecture-decisions)) | `authority_is_staged_under_the_pending_operation_before_it_is_removed`, `accepted_create_survives_with_the_servers_content`, `clean_extra_authority_is_written_and_kept` | covered | none |
| Receipt and page in either order leave the same state; an equal stamp compares against the held base; newer channel authority is not regressed by an older receipt; a later unsent edit replays over the returned authority (A4, A1) | `channel_first_then_receipt_dedups_and_still_completes`, `receipt_first_then_channel_is_a_no_op_that_advances_the_cursor`, `newer_channel_authority_is_not_regressed_by_an_older_response`, `later_unsent_edit_replays_over_the_returned_authority`; [push.rs](../../../../crates/sqlite/tests/push.rs) `pull_before_ack_and_later_local_edit_replay_in_order`; sim `reordered_receipt_and_page_agree_in_either_order`, `deletion_completes_from_the_receipt_and_reaches_a_peer_at_the_same_stamp` | covered | none |
| A record repeated in one batch completes from one final result; unrelated records are untouched; a sibling rejection is rolled back beside an accepted mutation; an all-rejected batch removes optimism and keeps direct edits | `repeated_record_in_one_batch_completes_from_one_final_result`, `unrelated_records_are_unaffected`, `failed_sibling_mutation_is_rolled_back_beside_the_accepted_one`, `all_rejected_batch_removes_optimism_and_keeps_direct_edits` | covered | none |
| Rejection removes the mutation and its lifecycle dependents, keeps a durable inbox until dismissed (P5) | sim `p5_rejection_rolls_back_and_rejects_dependents`; `rejection_removes_optimism_preserves_direct_truth_and_has_durable_inbox`; `schema_cascade_is_optimistic_same_fate_and_not_extra_wire_operations` | covered | none |
| A duplicate receipt changes nothing, before and after reopen; a receipt for another client or batch, or one that omits an accepted record's authority, is refused whole and the batch stays frozen; a receipt record whose state does not fit is skipped and reported and the batch completes; a deletion in the receipt removes the row and retains the stamp (A5, D5) | `duplicate_receipt_is_ignored_and_completion_survives_restart`, `a_receipt_that_cannot_be_applied_is_refused_and_the_batch_stays_frozen`, `a_receipt_record_that_does_not_fit_is_skipped_and_the_batch_completes`, `deletion_authority_removes_the_row_and_retains_the_stamp`; [push.rs](../../../../crates/sqlite/tests/push.rs) `record_status_reports_phases_and_duplicate_ack_is_idempotent`; sim `a_duplicated_receipt_is_a_no_op` and the `completed work had a matching response` invariant | covered | none |
| Companions settle locally except where the server answered; a companion cascade does not resurrect descendants; accepted wire rows do not promote a companion over the server's row | `companions_settle_locally_except_where_the_server_answered`; [push.rs](../../../../crates/sqlite/tests/push.rs) `accepted_companion_cascade_does_not_resurrect_descendants`, `accepted_wire_rows_do_not_promote_companion_over_server_authority` | covered | A server-side cascade the handler does not register is not in the receipt ([Settlement §11](../../architecture/client/engine/settlement.md#11-risks-and-technical-debt)); no test asserts the channel later correcting it. |
| Unsubscribing does not touch settlement: pending operations and before images are retained | [downlink.rs](../../../../crates/sqlite/tests/downlink.rs) `unsubscribing_retains_records_and_later_pages_are_dropped` (pending edits and before images asserted) | covered | none |

### Frontend interface

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Committed state survives reopen (L2); a stale handle cannot commit (R4) | [client.rs](../../../../crates/sqlite/tests/client.rs) `open_creates_tables_persists_identity_and_survives_reopen`, `stale_writer_cannot_overwrite_committed_database` | covered | The R4 test fences a write that would otherwise succeed; it does not show a lost update, which is fine, but the name overstates it. |
| A database from the checkpoint era is refused untouched when opened as a store, and rebuilt beside when opened through a path ([Reconciliation](../../architecture/client/storage/reconciliation.md)) | [ddl.rs](../../../../crates/sqlite/tests/ddl.rs) `a_database_from_the_checkpoint_era_is_refused_untouched` (the rows are counted after the refusal); [rebuild.rs](../../../../crates/sqlite/tests/rebuild.rs) `an_earlier_framework_layout_is_rebuilt_beside_not_refused` | covered | Nothing migrates an old file; its queue is counted as left behind. |
| `syncState().schema` reports a pending old file, `rebuild` is refused while work is unsent and switches the handle afterwards, the report names what stayed behind, reads on the fresh file are empty ([Reconciliation](../../architecture/client/storage/reconciliation.md)) | [session.rs](../../../../bindings/common/tests/session.rs) `incompatible_schema_reports_pending_work_and_rebuild_switches_files`; [rebuild.test.mjs](../../../../integration/bindings/client-js/rebuild.test.mjs); [client_test.dart](../../../../packages/dart/test/client_test.dart) `an incompatible schema keeps unsent work in the old file until rebuild is asked to leave it` | covered | The SDKs pass `discardPending` through; the switch is never automatic. |
| Watchers fire only for named tables and only after commit | `watch_fires_only_for_declared_tables`, `session_reads_own_writes…` | covered | none |
| Session API: reads inside see the session; sync commands are refused while open; unclosed savepoints refuse commit | `session_reads_own_writes…` (apply_page refused); [session.rs](../../../../bindings/common/tests/session.rs); [client.rs](../../../../crates/sqlite/tests/client.rs) `committing_with_an_unclosed_savepoint_is_refused_and_rolls_back` | covered | none |
| `drop_mutation` refuses a frozen mutation; dropping an unsent one leaves a `dropped` rejection and replays the rest | `frozen_mutations_cannot_be_dropped_and_unknown_dependencies_are_refused` | covered | This is the boundary of the "poisoned batch" limitation ([Batching §11](../../architecture/client/engine/push/batching.md)). |

### Connection controller (Rust parts)

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Backoff is bounded, wake does not busy-loop, pause and stop behave ([Scheduling](../../architecture/client/connection/controller/scheduling.md)) | unit tests in [connection.rs](../../../../crates/client/src/connection.rs) | covered | none |
| Lanes have independent retry state; the push-only cycle never pulls; the full cycle pulls every subscribed channel in one request | [session.rs](../../../../bindings/common/tests/session.rs) `downlink_and_push_drivers_have_independent_lifecycle_and_retry_state`, `live_push_cycle_keeps_receipts_but_leaves_reads_to_the_stream`, `rust_selects_transport_actions_and_reuses_frozen_request_on_retry` (the receipt completes the push without a pull; a page carrying the same authority is a no-op that advances the cursor); [query.rs](../../../../crates/sqlite/tests/query.rs) `transport_pulls_only_subscribed_channels_and_the_receipt_completes_the_push` | covered | The last test lives in a query file; leave it, but it is a controller test. |
| Downlink worker transitions: enqueue commits nothing and one pump commits one page, subscribe, heads in the acknowledgement and no catch-up when they equal the cursors, a bounded page queue whose gap pages are held while control work flows, one request at a time, full pages continue, gap and overflow recovery, subscription change, reconnect with backoff, protocol violations, pause, resume, stop, fenced events of a replaced socket and answers of an abandoned request ([Downlink worker](../../architecture/client/connection/controller/downlink-worker.md)) | [sqlite/tests/downlink_worker.rs](../../../../crates/sqlite/tests/downlink_worker.rs) (19 tests, scripted host, real SQLite); the `downlink` command in [session.rs](../../../../bindings/common/tests/session.rs) `incoming_pages_share_cursor_policy_and_do_not_overwrite_push_cycle`, `incoming_overlap_is_identical_with_or_without_http_request_metadata` | covered | Executed 2026-09-25: `cargo test -p axton-sqlite --test downlink_worker --locked`, `cargo test -p axton-binding --locked`. Real sockets, HTTP and credential refresh stay under [Connection integration](../integration/connection.md). |

Host-side behavior (real sockets, cancellation, refresh) is under [Connection integration](../integration/connection.md).
