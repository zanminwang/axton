# Storage and persistence tests

Verify real transaction boundaries, committed-reader isolation, rollback, reopen and concurrent access. Simulation's in-memory server host cannot establish PostgreSQL semantics.

| Boundary | Existing evidence |
| --- | --- |
| SQLite | [store.rs](../../../../crates/sqlite/tests/store.rs), [ddl.rs](../../../../crates/sqlite/tests/ddl.rs), and reopen scenarios in [client.rs](../../../../crates/sqlite/tests/client.rs) |
| PostgreSQL | [driver-conformance.test.mjs](../../../../integration/persistence/server/driver-conformance.test.mjs) once per shim (`pg`, `prisma`, `drizzle`), [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs), [actions.test.mjs](../../../../integration/persistence/server/actions.test.mjs), [membership.test.mjs](../../../../integration/persistence/server/membership.test.mjs) and the [transaction probe](../../../../integration/persistence/transaction-probe/README.md) |

After the prerequisites in [Running tests](../running.md):

```sh
cargo test -p axton-sqlite --test store --locked
cargo test -p axton-sqlite --test ddl --locked
bash integration/persistence/server/run.sh
bash integration/persistence/transaction-probe/run.sh
```

Use the real database and assert what survives a commit, rollback or reopen. Next review: isolation assumptions, simultaneous clients and schema changes with a non-empty pending queue.

## Coverage review

Reviewed 2026-09-14; tests read, not executed, except where a row says otherwise. SQLite rows executed 2026-09-15 by `cargo test -p axton-sqlite --locked`; PostgreSQL rows executed 2026-09-26 by `bash integration/persistence/server/run.sh` inside `bash scripts/test.sh`, PostgreSQL 14.23, with the driver package and the `touch`/`channel` declaration API.

### SQLite (client store and reconciliation)

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Reader sees only committed rows; writer sees its own; savepoints nest; read paths refuse writes; second writer fails after the busy timeout ([Store](../../architecture/client/storage/store.md)) | [store.rs](../../../../crates/sqlite/tests/store.rs) | covered | none |
| Fresh database: model, before and framework tables, unique indexes | [ddl.rs](../../../../crates/sqlite/tests/ddl.rs) `creates_model_before_and_framework_tables` | covered | none |
| Additive changes: nullable column, non-nullable with default; unknown columns kept ([Reconciliation](../../architecture/client/storage/reconciliation.md)) | `adds_missing_columns_to_both_tables_and_keeps_unknown_ones` | covered | none |
| `reconcile` refuses a non-nullable column without default, a type change, an identity change; file untouched | `rejects_non_nullable_column_without_default_identity_change_and_type_change` | covered | Through `open_at` these are classified before `reconcile` runs and rebuild beside; the refusal is the store-level guard. |
| A checkpoint-era layout (`axton_push_checkpoint`, `axton_claim`, or `axton_client` without `last_completed_push`) opened as a store is refused before any DDL and left untouched; opened through a path it is rebuilt beside ([Reconciliation](../../architecture/client/storage/reconciliation.md)) | `a_database_from_the_checkpoint_era_is_refused_untouched`; [rebuild.rs](../../../../crates/sqlite/tests/rebuild.rs) `an_earlier_framework_layout_is_rebuilt_beside_not_refused` | covered | The check reads `sqlite_master` and `PRAGMA table_info` on the committed reader; no write path is reached on the old file. |
| Compatibility: identical and additive open in place and update the descriptor; incompatible rebuilds to `<path>.1` with the sidecar, old file kept, the Scope names carried over as fresh uninitialized subscriptions; an abandoned `<path>.1` is removed and the retry takes `<path>.2`; a descriptor-less file adopts its schema ([Reconciliation](../../architecture/client/storage/reconciliation.md)) | [rebuild.rs](../../../../crates/sqlite/tests/rebuild.rs) `unchanged_and_additive_schemas_open_in_place`, `an_incompatible_schema_gets_a_fresh_file_and_keeps_the_old_one`, `a_subscription_table_without_identities_is_rebuilt_beside`, `an_abandoned_partial_rebuild_is_removed_and_retried`, `a_current_layout_file_without_a_descriptor_adopts_the_schema_it_opens_with`; every rule's reason in [core/tests/compatibility.rs](../../../../crates/core/tests/compatibility.rs) | covered | A changed unique set, relation, removed model or changed enum are incompatible by rule, so the former "kept but undeclared" behaviour is gone. |
| Unsent work keeps the old file open with its stored schema and unchanged frozen bytes; `rebuild` refuses until sent, then switches the handle; discarding reports mutations and direct records left behind | `unsent_work_keeps_the_old_file_open_until_it_is_sent_then_rebuild_switches`, `discarding_pending_work_reports_what_the_old_file_keeps` | covered | A direct record is a row with no stamp and no pending operation. |
| Reconciliation with a populated queue: frozen bytes unchanged, unsent creates still valid | [push.rs](../../../../crates/sqlite/tests/push.rs) `populated_queue_survives_additive_reconciliation_with_frozen_bytes_unchanged` (reopen with an added nullable column: the in-flight batch's bytes are identical, both mutations stay pending, the added column reads as null, the unsent create is sent without the new field) | covered | none |
| Creation defaults ([Models](../../architecture/schema/models.md#9-architecture-decisions)): generated once before persistence; optimism, stored args and frozen bytes agree and survive reopen; rollback discards them; a default-only change opens in place without rewriting rows, args or frozen bytes; a creation default never backfills a new required column | [defaults.rs](../../../../crates/sqlite/tests/defaults.rs) | covered | Real SQLite; PostgreSQL and both handler routes in [action.test.mts](../../../../integration/action-e2e/action.test.mts). |
| Reopen preserves queue, completion counter, frozen declaration, cursors, stamps (deleted records included), retained records and rejections (L2, R3) | [client.rs](../../../../crates/sqlite/tests/client.rs), [push.rs](../../../../crates/sqlite/tests/push.rs), [settlement.rs](../../../../crates/sqlite/tests/settlement.rs) `duplicate_receipt_is_ignored_and_completion_survives_restart`, `frozen_batch_and_its_declaration_survive_restart_until_completed`, [downlink.rs](../../../../crates/sqlite/tests/downlink.rs) `another_channel_updates_retained_content_and_restart_keeps_it`, [stamp_scenarios.rs](../../../../crates/sqlite/tests/stamp_scenarios.rs) `reopen_preserves_stamps_and_tombstones` | covered | A reopen is a close and open, not an interrupted process; see [Failure and recovery](../simulation/recovery.md). |

### PostgreSQL (server persistence)

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Every shim answers claim (create, lock, replay), receipt ownership, `head`, `advanceStamp`, `ensureStamp` under three concurrent transactions, `publish` stamp validation, rollback of a first initialisation, the `scan` join and missing metadata, savepoints and a serialization retry the same way ([Persistence §10](../../architecture/server/persistence.md#10-quality-requirements)) | [driver-conformance.test.mjs](../../../../integration/persistence/server/driver-conformance.test.mjs), the same nine tests for `pg`, `prisma` and `drizzle`, plus `pgDriver retries only serialization failures, a bounded number of times` | covered | The suite runs in its own `node --test` invocation before `runtime.test.mjs`, because parallel test files applying the migration to one cluster race on table creation. |
| Client row lock serializes concurrent retries; receipt stored atomically with business writes (P1, P6) | [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs) `concurrent same-client retry executes once under PostgreSQL lock`, `concurrent same-client delivery with different bodies commits at most one under the PostgreSQL lock` (the pair shares one receipt, one handler call and one channel head increment), `push commits business + compacted publication + exact durable receipt together`, `a handler that breaks the transaction still fails the delivery` | covered | none |
| Per-mutation savepoints against a real database | `explicit rejection rolls back only mutation and its publication`; [transaction-bridge.test.mjs](../../../../integration/bindings/node/transaction-bridge.test.mjs) `business rejection rolls back its savepoint while preceding mutation commits` | covered | The bridge test drives `SAVEPOINT` statements by hand around the probe build (`axton-node-probe.node`), not through `createBackend`; the first test is the production path. |
| A thrown handler or loader error is recovered by the savepoint into that mutation's rejection (`handler.failed`/`loader.failed`) against a real database and a real aborted transaction, reported to `onError`; a mixed batch commits the unsupported-version mutation's neighbors; an invalid declaration is the same kind of failure (P6, P7; [Backend interface §6](../../architecture/server/backend-interface.md#6-runtime-view)) | [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs) `a handler that throws rejects only its mutation and reaches onError`, `a handler whose SQL fails leaves an aborted transaction that the savepoint rollback recovers into one rejection`, `a loader that throws during readback rejects only that mutation`, `a mixed batch rejects the unsupported version alone and commits the rest`, `a declaration validates its Channel and records at the call; the invalid call rejects only that mutation as a failure` | covered | Executed 2026-09-26 by `bash integration/persistence/server/run.sh` inside `bash scripts/test.sh`, PostgreSQL 14.23. |
| Stamps and cursors are separate counters: `advanceStamp` increments without a channel, one push to two channels carries one stamp and advances each head once, an external write advances the stamp per call while a push re-enrolling an unchanged member does not, concurrent first publications initialize one stamp of 1, a rollback removes a first initialization with its publication, `publish` refuses a missing or stale stamp, `scan` pairs the cursor with the current stamp and a missing record row is a storage defect (D3; [Persistence §10](../../architecture/server/persistence.md#10-quality-requirements)) | `advanceStamp increments without a channel: no invalidation, no channel head`, `one push enrolled in two channels carries the same stamp to both and advances each head once`, `an external write advances the stamp on every call; a push re-enrolling an unchanged member neither advances nor publishes it`, `concurrent first publications initialise one stamp of 1 and never overwrite an established one`, `a rolled-back transaction removes a first initialisation together with its publication`, `publish refuses a record without metadata or with a stamp that is not its current one`, `scan pairs the invalidation cursor with the current record stamp; a missing record row is a storage defect`, `concurrent notifies of one record receive distinct stamps` | covered | Executed 2026-09-26 by `bash integration/persistence/server/run.sh` inside `bash scripts/test.sh`, PostgreSQL 14.23. |
| One pull covers every channel and a record shared by two channels arrives once; a loader that throws or refuses for one id fails only that record, as an `error` change, and reaches `onError`, which defaults to `console.error`; a malformed, misaligned or unserializable answer fails only its records too (D7; [Server Pull](../../architecture/server/engine/pull.md)) | `a pull covers every channel in one request and delivers a record shared by two channels once`, `a loader that throws for one id fails only that record and reaches onError`, `a loader refusal for one id is an error change carrying the refusal code`, `onError defaults to console.error so nothing is dropped silently`, `loader defects fail only their records; the page is served and the cursor advances` | covered | Executed 2026-09-16: `bash integration/persistence/server/run.sh`. |
| Head, scan and load coherent under concurrent publication | `repeatable-read runner keeps head, scan, and loader coherent…` | covered at RepeatableRead | none |
| Serialization-failure retry in the shims: only `P2034` and raw `40001`/`40P01` retry, bounded by `retries` (default 3 more attempts), the last failure is reported, the body runs once per attempt | `pgDriver retries only serialization failures, a bounded number of times` and the per-shim `transaction retries a serialization failure and rolls back on throw` in [driver-conformance.test.mjs](../../../../integration/persistence/server/driver-conformance.test.mjs); `a RepeatableRead conflict on the real database retries the whole body once and commits it exactly once` (a concurrent committed update to the row the body updates: the body runs twice, the retry sees the new snapshot, the counter ends one higher than the concurrent write; with `retries: 0` the conflict is reported and nothing is written) | covered | Verified 2026-09-16 by `bash integration/persistence/server/run.sh`. The real-database case provokes PostgreSQL's `could not serialize access due to concurrent update`; a deadlock (`40P01`) is exercised only through the fake. |
| Channel membership on a real database ([Publish](../../architecture/server/engine/publish.md), [Pull](../../architecture/server/engine/pull.md)): removed members are filtered before the scan limit in delta and Bootstrap pages, all-hole ranges still advance, re-adding publishes current state at a fresh position without a new stamp, deleted members yield `null` and recreated identities are distributed again, removal fabricates no deletion; touch/add and touch/remove race in both commit orders, membership-only races keep the stamp, and rejected or failed transactions roll back relationships, stamps, heads and wakes | [membership.test.mjs](../../../../integration/persistence/server/membership.test.mjs), every test in the file | covered | `pg` shim only (the shims share every SQL statement, and [driver-conformance.test.mjs](../../../../integration/persistence/server/driver-conformance.test.mjs) runs the membership operations on all three). Races force both orders with held snapshots; a released-together membership-only pair commits in one order. Executed 2026-09-26 inside `bash scripts/test.sh`, PostgreSQL 14.23; CI runs 16. |
| BigInt narrowing to safe integers | `loader safely converts PostgreSQL BigInt scalar and list values…` | covered | none |
| A refused declaration prevents commit and rolls the business write back; in a push it rejects only that mutation, including an identity the engine would refuse (the collector applies the engine's UUID, enum and date-time rules) | `backend.transaction rolls the business write back when a declaration is refused`, `a declaration naming an unknown Model fails its handler: only that mutation and its write roll back`, `a malformed UUID declared in a push rejects only its mutation; well-formed identities settle beside it` | covered | none |
| The TypeScript host answers every host operation exactly as the Rust contract defines it ([Backend interface §9](../../architecture/server/backend-interface.md)) | [host-contract.test.mjs](../../../../integration/persistence/server/host-contract.test.mjs) `the fixture and the TypeScript union cover the same operations`, `every fixture request replays through the TypeScript host to the fixture answer`, `the same handle request settles as the fixture rejection when the handler refuses`, `the PostgreSQL persistence answers the persistence half through a two-method driver and refuses application operations` (twelve operations since `advanceStamp`, `ensureStamp` and the stamped `publish`) | covered | It replays [host-operations.json](../../../../fixtures/protocol/host-operations.json) against fake native and fake persistence, so it proves the two hosts agree on shape, not database behavior; it runs in `run.sh` beside `runtime.test.mjs` because that is where the server host is already built. |
| Fresh tables define no `request_hash`; a table still carrying the column (installed before its removal) claims, replays and refuses gaps unchanged; re-applying `migration.sql` leaves the existing table alone; dropping the column keeps the stored receipt ([Persistence §11](../../architecture/server/persistence.md)) | `fresh framework tables omit request_hash; a table that still carries the column keeps replaying receipts` | covered | Verified 2026-09-26 by `bash integration/persistence/server/run.sh` inside `bash scripts/test.sh`, PostgreSQL 14.23. The test adds and drops the column on the suite's own table. |
| External writes: `backend.transaction` hands its body `channel` and `touch`, stamps and publishes in the application transaction and wakes only after commit; a failing body rolls back and wakes nobody; a membership-only record keeps its stamp and a touch without a membership declaration still stamps it; a connected live subscriber is woken without reconnect ([Publish §9](../../architecture/server/engine/publish.md#9-architecture-decisions)) | `backend.transaction publishes in the application transaction and wakes after commit`, `backend.transaction rolls back a failing body and wakes nobody`, `re-adding an unchanged member keeps its stamp and publishes nothing; a later touch reaches its Channel without another add`, `backend.transaction wakes a connected live subscriber without reconnect`, `notify and bindTransaction are gone; backend.transaction is the only external write path` | covered | Executed 2026-09-16 by `bash integration/persistence/server/run.sh` (68 passed). |
