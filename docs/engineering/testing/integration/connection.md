# Connection tests

Verify protocol messages and lifecycle across HTTP/WebSocket: subscription acknowledgement, initial catch-up, streamed updates, cancellation, reconnect and authentication refresh.

[JavaScript live tests](../../../../integration/bindings/client-js/live.test.mjs) and [Dart live tests](../../../../packages/dart/test/live_test.dart) cover the client side. [Server runtime tests](../../../../integration/persistence/server/runtime.test.mjs) include live subscriptions and after-commit wakes. Some lifecycle tests use controlled transports; distinguish those from real socket tests.

After setup in [Running tests](../running.md):

```sh
node --test integration/bindings/client-js/live.test.mjs
bash integration/persistence/server/run.sh
```

Assert that obsolete responses cannot affect the current session and that overlapping catch-up/stream pages preserve local state. Test the existing connection model: HTTP catch-up with WebSocket updates.

Next review: map cancellation, overflow, auth and reconnect scenarios across both clients, including teardown and resource cleanup.

## Coverage review

Reviewed 2026-09-14; tests read, not executed, except where a row records a verified run. Verified 2026-09-15 after adding the Dart byte-bound overflow test: `node --test integration/bindings/client-js/live.test.mjs` passed (19 tests) and `(cd packages/dart && dart analyze && dart test)` reported no analyzer issues and passed (28 tests). Client-side tests use a real WebSocket server and real HTTP listener in the test process but a scripted backend; server-side tests run the real backend over PostgreSQL. Both count as real transport boundaries; neither alone proves the full pair, which is the end-to-end suite's job.

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| The socket sends the subscribe frame and delivers frames in order; cancellation ends stalled tokens, in-flight requests and opening handshakes ([Transport](../../architecture/client/connection/transport.md)) | [live.test.mjs](../../../../integration/bindings/client-js/live.test.mjs) first five tests; [live_test.dart](../../../../packages/dart/test/live_test.dart) first five tests | covered in both languages | The acknowledgement is checked by Rust ([Live session](../../architecture/client/connection/controller/live-session.md)); the transport tests only assert delivery. Verified 2026-09-15: `node --test integration/bindings/client-js/*.test.mjs` (32 pass), `dart test` (28 pass), `bash integration/e2e/run.sh`. |
| Catch-up starts only after the acknowledgement and only when a cursor is behind its acknowledged head; steady-state streaming does not poll HTTP ([Live session](../../architecture/client/connection/controller/live-session.md)) | `unified connection acknowledges listeners then catches up through HTTP before live delivery`; Dart `HTTP catch-up pages after ack, queues overlap, and rejects obsolete HTTP completion` | covered | none |
| Duplicates covered, overlaps applied without HTTP, gaps recovered | `one incoming page path covers duplicates, applies overlap directly and recovers genuine gaps`; Dart `native subscription changes discard old pages and HTTP recovers live gaps` | covered | none |
| Subscription changes invalidate the session and pending authentication; obsolete pages and late catch-ups are discarded | `client replaces subscriptions…`, `late HTTP catch-up after unsubscribe and resubscribe…`, `subscription invalidation cancels pending authentication…`; Dart `unsubscribe invalidates a pending token before a held transaction drains` | covered | none |
| Auth refresh on 401, retried after a failed refresh; concurrent 401s on both lanes share one refresh ([Scheduling](../../architecture/client/connection/controller/scheduling.md)) | `client retries upgrade authentication and survives failed refresh`; Dart equivalent; `a 401 on both lanes at once shares one refreshAuth; both lanes recover with the new token` in [live.test.mjs](../../../../integration/bindings/client-js/live.test.mjs) and [live_test.dart](../../../../packages/dart/test/live_test.dart) | covered in both SDKs | The shared-refresh test holds `refreshAuth` on a gate while a push and an upgrade are both refused with `401`: each lane is refused once, `refreshAuth` runs once, and after the gate both lanes proceed with the new token and the pending mutation completes. The JS assertion fails when the shared promise is removed (checked once by hand). Verified 2026-09-14: `node --test integration/bindings/client-js/live.test.mjs` (18 pass) and `dart test` (`live_test.dart`, 14 pass). |
| A 403 identity refusal on a push reaches `onError` with its code and leaves the batch frozen for a resend, since the server never ran it (P6/P7, [Server Push §9](../../architecture/server/engine/push.md#9-architecture-decisions)) | `an owner-mismatch refusal reaches onError and leaves the batch frozen for a resend` in [live.test.mjs](../../../../integration/bindings/client-js/live.test.mjs) and [live_test.dart](../../../../packages/dart/test/live_test.dart) | covered in both SDKs | Verified 2026-09-15: `node --test integration/bindings/client-js/*.test.mjs` (33 pass), `dart test` (29 pass). |
| Bounded buffer overflow recovers without restarting HTTP | `bounded receive overflow preserves in-flight HTTP progress and recovers the latest head`; Dart `bounded receive buffer (128 pages) overflows into recovery without restarting the in-flight HTTP catch-up` and `bounded receive buffer (8 MiB) overflows on ten large pages without restarting the in-flight HTTP catch-up` | covered in both SDKs; both Dart bounds exercised | Each test holds the first catch-up, then floods the socket: 200 small pages for the page bound, ten pages of just over 1 MiB for the byte bound. In both the socket is not restarted, recovery coalesces into at most four pulls, and the head only HTTP can deliver is reached. Only Dart bounds bytes ([Transport §5](../../architecture/client/connection/transport.md)); the TypeScript buffer bounds 64 pages and pauses the socket while a page is applied, and its 8 MiB is the `ws` per-frame `maxPayload`, not a buffer budget, so it has no byte-bound overflow to exercise. That frame limit itself is untested in both SDKs. |
| Pause cancels held requests and a late token cannot start one; resume reconnects | `pause cancels held catch-up and a late HTTP token cannot start a request`; Dart `pause blocks a selected parent request before awaiting child pause`, `shared live factory isolates cancellation…` | covered | none |
| Push completes from its receipt while the WebSocket cannot be established; no pages arrive; once the upgrade is allowed, HTTP catch-up runs once and its page is the same or newer authority ([Controller decision](../../architecture/client/connection/controller/README.md)) | [live.test.mjs](../../../../integration/bindings/client-js/live.test.mjs) `push completes from its receipt while the WebSocket upgrade is refused; HTTP catch-up runs only once the upgrade is allowed`; Dart [live_test.dart](../../../../packages/dart/test/live_test.dart) same name | covered in both SDKs (read) | The server refuses upgrades with `503` and serves `/sync/mutations`: the push is accepted and pending drops to 0 with the receipt's state visible, no `/sync/pull` runs (no polling fallback), the refusals reach `onError`, the lane keeps retrying; after upgrades are allowed the catch-up runs once, the receipt is not re-requested, and the page's newer stamp lands. Characterizes the decision. Both suites executed 2026-09-15 after the change (`node --test integration/bindings/client-js/*.test.mjs`, 32 passed; `dart test`, 28 passed). |
| Records a page cannot apply reach `onError` as an `AxtonReport` (read failure, skipped change, divergence); a diverged edit is still sent and completes from its receipt (D7, D8; [Pull](../../architecture/client/engine/pull.md)) | `what a page cannot apply reaches onError as an AxtonReport: read failures, skipped changes and divergence`, `a queued edit whose replay fails over new authority is reported as diverged and still sent` in [live.test.mjs](../../../../integration/bindings/client-js/live.test.mjs); Dart [live_test.dart](../../../../packages/dart/test/live_test.dart) covers both in the first of those names | covered in both SDKs | Executed 2026-09-16 with the pull protocol change: `node --test integration/bindings/client-js/*.test.mjs` (35 pass), `dart test` (30 pass). |
| Server: authenticate before upgrade, one subscribe frame, an acknowledgement with heads, wake after commit, one pull for every channel, page split, reconnect, duplicate receipt is silent, shutdown closes sockets | [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs) `live transport negotiates, wakes only after commit, reconnects, and cleans up`, `listen answers pull over HTTP…` | covered | A drain error closing with `1011` and a failed negotiation closing with `1002` are asserted in `HTTP classifies native failures by code…` (a fake native that answers the `{handle, actions}` / `liveEvent` / `liveClose` surface). Verified 2026-09-15 by `bash integration/persistence/server/run.sh` after the drain policy moved to the Rust `Subscriptions` controller. |
| Server: a publication committed between the negotiation transaction and the acknowledgement is delivered by the first drain ([Controller §6](../../architecture/server/connection/controller.md)) | `a publication committed between negotiation and the acknowledgement is delivered by the first drain` | covered | A gated `database.transaction` holds the negotiation result while a push commits; nothing is sent before the gate opens, then the acknowledgement and a page carrying the pushed record arrive. Verified 2026-09-15 by `bash integration/persistence/server/run.sh`. |
| Server: upgrade refusals — no or unknown credentials `401`; authentication that completes after `close()` began `503` ([Transport](../../architecture/server/connection/transport.md)) | `an upgrade whose authentication completes after close begins is refused with 503; missing and invalid credentials are refused with 401` (an `authenticate` held on a gate while `close()` starts; the socket's error carries the status); the stripped-header `401` in the reverse-proxy test | covered | Verified 2026-09-14 by `bash integration/persistence/server/run.sh`. The `503` for an upgrade arriving after `close()` is unreachable: `close()` removes the upgrade listener first. |
| Server behind a reverse proxy: HTTP forwarded, WebSocket upgrade relayed at the TCP level, `Authorization` preserved; a stripped header is `401` and a refused upgrade ([Transport §7](../../architecture/server/connection/transport.md)) | `a reverse proxy forwarding HTTP and the WebSocket upgrade with headers serves push, pull and live; a stripped Authorization header is refused` | covered (in-process proxy) | TLS termination and real proxy products are not exercised; see [Deploy the backend](../../../../website/docs/backend/deployment.md). |
| Reconnect after the server closes the socket, with backoff ([Scheduling](../../architecture/client/connection/controller/scheduling.md)) | `a socket the server closes is reconnected after the backoff, resubscribed, and streaming resumes` in [live.test.mjs](../../../../integration/bindings/client-js/live.test.mjs) and [live_test.dart](../../../../packages/dart/test/live_test.dart); e2e pause/resume | covered in both SDKs | The server closes the acknowledged socket with `1001`; asserted: the second upgrade arrives at least 180 ms later (the first retry is due after 250 ms minus 20% jitter) without an application event, the close reaches `onError`, the new socket sends a subscribe frame, a page on it applies, and there is exactly one reconnect. Only the first retry's floor is asserted; the doubling and cap are the Rust unit test's evidence. |
