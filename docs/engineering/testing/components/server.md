# Server tests

Verify request validation, duplicate-batch handling, rejection, readback of changed records, loader results and publication. See [Server architecture](../../architecture/server/README.md).

[Existing Rust tests](../../../../crates/server/tests) provide a host implementation so each test can control responses and inspect calls.

```sh
cargo test -p axton-server --locked
```

Assert both the response and the business calls that are permitted or prevented. A host fixture can establish sequencing, but real database locking and rollback need [Persistence tests](../integration/persistence.md).

Next review: map host-fixture assertions to server requirements and identify cases covered only by the PostgreSQL suite.

## Coverage review

Reviewed 2026-09-14 against the server architecture documents; tests read, not executed. The push flow (stamps, readback, publication order, rejection versus failure) is Rust and proven against an in-process host in [readback.rs](../../../../crates/server/tests/readback.rs), executed 2026-09-15 by `cargo test -p axton-server --locked`; the TypeScript host's own logic (the handler's `changes` and `publish` collection, `translateRejection`, wake sets) is proven only in the PostgreSQL suite [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs); those rows are listed here because they are component rules, even though the environment is an integration one.

| Behavior | Existing tests | Coverage | Gap and next step |
| --- | --- | --- | --- |
| Config validation: descriptors, patch capabilities, slot shapes; every mutation version has a handler and every model a loader ([Backend interface](../../architecture/server/backend-interface.md)) | [runtime.rs](../../../../crates/server/tests/runtime.rs) `startup_rejects_invalid_patch_capabilities`; `backend validates config and complete registrations at startup` | covered | none |
| One typed host-operation contract: the operation list matches the request enum, every request and response round-trips, an unknown field or a wrong-typed response is refused per operation, a `handle` answer carries changes and publications or a rejection and never both, a `load` answer is rows or a refusal code; twelve operations including `advanceStamp`, `ensureStamp` and `publish` with a stamp ([Backend interface §9](../../architecture/server/backend-interface.md)) | [host_contract.rs](../../../../crates/server/tests/host_contract.rs) `the_operation_list_matches_the_request_enum`, `the_fixture_covers_every_operation_exactly_once`, `every_fixture_request_and_response_round_trips`, `a_request_missing_a_field_or_carrying_an_unknown_one_is_refused`, `a_response_carrying_an_unknown_field_is_refused`, `a_response_of_the_wrong_type_is_refused_per_operation`, `a_handle_response_carries_changes_and_publications_or_a_rejection_and_never_both`, `a_load_response_is_rows_or_a_refusal_code`, `counters_keep_their_range_and_name_themselves`, `an_unusable_response_names_its_operation_and_ordinal` | covered | Rust proves both directions against [host-operations.json](../../../../fixtures/protocol/host-operations.json); the TypeScript host is held to the same fixture by [host-contract.test.mjs](../../../../integration/persistence/server/host-contract.test.mjs) ([Persistence](../integration/persistence.md)). |
| Versioned handler registration: every retained version named, a bare function means v1 only, unknown `v<n>` and non-function values refused; a version reaches only its own handler ([Typed API / Server](../../architecture/sdks/typed-api/server.md)) | `handler registration names every retained version and a function means v1 only`, `a version dispatches only to its own handler and a function registers v1` | covered | Verified 2026-09-15 by `bash integration/persistence/server/run.sh` (66 passed) with the `changes`/`publish` handler API. |
| Versioned loader registration and dispatch: every retained model version named, a bare function means v1 only, a schema whose current version is not retained is refused; a pull reaches only the served version's loader and its rows are normalized by that version's contract ([Typed API / Server](../../architecture/sdks/typed-api/server.md), [Server / Pull](../../architecture/server/engine/pull.md)) | `loader registration names every retained model version and a function means v1 only`, `a pull reaches the loader of the declared model version and normalizes rows with that contract`; [stamp.rs](../../../../crates/server/tests/stamp.rs) `pull_normalizes_loader_rows_with_the_retained_contract_of_the_served_version`; [runtime.rs](../../../../crates/server/tests/runtime.rs) `startup_validates_the_retained_model_contracts` | covered | Verified 2026-09-15 by `cargo test -p axton-server --locked` and `bash integration/persistence/server/run.sh` (66 passed). The served version is the one the client declares; see the protocol rows for the declaration. |
| Argument decoding and rejection codes ([Mutations](../../architecture/schema/mutations.md)) | `ordered_slot_decodes_known_fields_and_ignores_new_fields`, `known_disallowed_patch_is_explicit_refusal`, `undeclared_operation_is_invalid`, `create_binding_mismatch_refuses_the_whole_act`, `historical_known_field_outside_capability_is_refused` | covered | Greedy adjacent slots: see [Schema](schema.md). |
| Same sequence returns the stored receipt without executing, whatever the body carries; gaps and overlaps refused (P1, P2; [Server Push §9](../../architecture/server/engine/push.md#9-architecture-decisions)) | [readback.rs](../../../../crates/server/tests/readback.rs) `a_retried_batch_answers_from_storage_without_running_a_handler`, `an_unretained_declaration_is_refused_before_claim`; sim `p1_lost_receipt_retry_executes_once_and_returns_the_stored_receipt`, `p2_contiguous_sequence_and_server_refuses_gap_and_overlap`; `push commits business + compacted publication + exact durable receipt together` (same and changed body: no handler call, business row and invalidation count unchanged, no `onCommitted` wake); `concurrent same-client retry executes once under PostgreSQL lock`; `concurrent same-client delivery with different bodies commits at most one under the PostgreSQL lock`; `fresh framework tables omit request_hash; …` (after sequence 2 commits, sequence 1 is `overlap` even with its original body) | covered | Rust and PostgreSQL rows executed 2026-09-15 (`bash integration/persistence/server/run.sh`, 66 passed). |
| An unsupported version, a handler failure, a loader failure and an undeclared or unretained changed model each reject only that mutation without invoking its handler (version) or aborting unrelated work; a mixed batch commits the rest; a failed `rollback` and a broken transaction still fail the whole delivery, and a retry after that runs the handlers exactly once (P6, P7; [Server Push §9](../../architecture/server/engine/push.md#9-architecture-decisions)) | [readback.rs](../../../../crates/server/tests/readback.rs) `an_unsupported_version_rejects_only_that_mutation`, `a_handler_failure_rejects_only_that_mutation`, `a_loader_failure_rejects_only_that_mutation`, `a_failed_rollback_fails_the_delivery`, `an_unretained_declaration_rejects_only_the_touching_mutation`; [runtime.rs](../../../../crates/server/tests/runtime.rs) `push_refusals_carry_stable_codes_and_run_no_handler` (in-process host fixture: code, no `handle` call); HTTP `409`/`500` bodies asserted in `HTTP maps engine codes to statuses…`; [runtime.test.mjs](../../../../integration/persistence/server/runtime.test.mjs) `a handler that throws rejects only its mutation and reaches onError`, `a handler whose SQL fails leaves an aborted transaction that the savepoint rollback recovers into one rejection`, `a loader that throws during readback rejects only that mutation`, `a handler that breaks the transaction still fails the delivery`, `a mixed batch rejects the unsupported version alone and commits the rest`; sim `p6_handler_failure_rejects_one_mutation_and_the_batch_commits`, `p6_a_broken_transaction_fails_the_delivery_and_the_retry_executes_once` | covered | Executed 2026-09-15: `cargo test -p axton-server --locked`, `cargo test -p axton-sim --locked`, `bash integration/persistence/server/run.sh` (66 passed). |
| Readback: each changed record is stamped once and read back once at that stamp, in the declared version; repeated operations on one record share a stamp; a later mutation replaces an earlier result and a later rejection keeps it; handler `changes` are read back and publication-only records are not; a deletion reads back as `null`; a quiet success still advances the stamp; a changed model the client did not declare rejects that mutation ([Server Push §10](../../architecture/server/engine/push.md#10-quality-requirements)) | [readback.rs](../../../../crates/server/tests/readback.rs) `success_reads_back_each_changed_record_once_at_its_stamp`, `repeated_operations_on_one_record_share_one_stamp`, `a_later_mutation_on_the_same_record_replaces_the_earlier_result`, `a_later_rejection_on_the_same_record_keeps_the_first_success`, `handler_changes_are_read_back_and_publication_only_records_are_not`, `a_deleted_record_reads_back_as_null`, `a_quiet_success_still_advances_the_stamp_and_reads_back`, `records_are_loaded_at_the_declared_version`, `a_changed_model_the_client_did_not_declare_rejects_that_mutation`, `handler_changes_naming_an_unregistered_model_are_an_error`; sim `overlapping_records_in_one_batch_complete_at_the_last_stamp`, `a_rejected_mutation_beside_an_accepted_one` | covered | Executed 2026-09-15: `cargo test -p axton-server --locked`. Simulation rows executed the same day by `cargo test -p axton-sim --locked`. |
| Handler API in the TypeScript host: a handler that publishes nothing still yields readback records; `publish({channel})` covers the final change set including later additions; explicit records publish only those; `publish` validates before settlement; the transaction stays usable after `publish`; an all-rejected batch has no records | `a handler that publishes nothing still returns readback records and touches no channel`, `publish({channel}) publishes the final change set, an addition made after the call included`, `explicit records publish only those, never join the change set, and are initialised at stamp 1 once`, `publish validates channel and records before the mutation settles`, `a handler may keep using the transaction after publish; the publication goes out with the commit`, `an all-rejected batch settles with no records`, `slot arguments are tagged so changes.add and publish accept them directly` | covered | Executed 2026-09-15 by `bash integration/persistence/server/run.sh` (66 passed). |
| Rejection rolls back one savepoint; a loader refusal in a push is that mutation's rejection; a `Failed` answer from either is `handler.failed`/`loader.failed` and an invalid readback row is `loader.invalid`, each rolled back alone; an infrastructure-level host error or a failed publication rolls back the batch, the client row and publications (P6) | [readback.rs](../../../../crates/server/tests/readback.rs) `a_loader_refusal_rejects_the_mutation_and_keeps_earlier_results`, `a_handler_failure_rejects_only_that_mutation`, `a_loader_failure_rejects_only_that_mutation`, `a_loader_host_error_fails_the_whole_push`, `invalid_loader_rows_reject_only_their_mutation`, `a_failed_publication_fails_the_push`; `explicit rejection rolls back only mutation and its publication`, `a loader refusal during push rejects only that mutation; so does a thrown loader error`, `publication failures poison push…`, `registered translator rejects one mutation; malformed translator code aborts transaction`; sim `p6_handler_failure_rejects_one_mutation_and_the_batch_commits` | covered | none |
| Owner mismatch, gap and overlap refused with codes (P2) | `push_refusals_carry_stable_codes_and_run_no_handler`; `push commits business…`; HTTP `403`/`409` in `HTTP maps engine codes to statuses…` | covered | none |
| Pull: the scan's current record stamp is copied into the change, positive stamp required, compaction, 50-row pages, loaders receive no channel, a malformed or misaligned answer fails only its records; one pull covers every channel and a shared record arrives once; a refused or failed read is an `error` change after a per-identity retry; `onError` defaults to `console.error` ([Server Pull](../../architecture/server/engine/pull.md)) | [stamp.rs](../../../../crates/server/tests/stamp.rs) `pull_copies_the_row_stamp_into_the_change`, `pull_rejects_rows_without_a_positive_stamp`, `one_pull_covers_every_channel_and_delivers_a_shared_record_once`, `a_full_channel_continues_independently_of_the_others`, `a_loader_refusal_isolates_one_record_after_a_per_identity_retry`, `a_loader_failure_is_an_error_change_and_a_single_record_needs_no_retry`, `a_thrown_host_error_still_fails_the_pull`, `a_malformed_loader_row_fails_only_its_record`, `a_misaligned_loader_answer_is_retried_per_identity`; `a pull covers every channel in one request and delivers a record shared by two channels once`, `a loader that throws for one id fails only that record and reaches onError`, `a loader refusal for one id is an error change carrying the refusal code`, `onError defaults to console.error so nothing is dropped silently`; `compaction materializes latest state…`, `50-row pages retain original cursor progression…`, `loaders receive no channel`, `loader defects fail only their records…`, `an undefined loader entry fails only its record…`, `a nonfinite loader value fails its record…`, `scan pairs the invalidation cursor with the current record stamp; a missing record row is a storage defect` | covered | none |
| Head, scan and load observe one snapshot | `repeatable-read runner keeps head, scan, and loader coherent across concurrent publication` | covered at RepeatableRead | Nothing tests an application runner at a weaker isolation level; the requirement is prose ([Persistence §11](../../architecture/server/persistence.md)). |
| Publish: a change allocates one stamp and every channel it is published to carries it; publishing an unchanged record reuses or initializes its stamp and never advances it; a publish naming a stale stamp is refused; the default publication is the final change set and `[]` publishes nothing; rejected mutations publish nothing; wakes only after commit (D3) | [stamp.rs](../../../../crates/server/tests/stamp.rs) `publish_advances_one_stamp_per_record_and_distributes_it_at_that_stamp`; [readback.rs](../../../../crates/server/tests/readback.rs) `default_publication_covers_the_final_change_set`, `explicit_empty_records_publish_nothing`, `a_publish_that_echoes_another_stamp_is_host_invalid`; `one push publishing to two channels carries the same stamp to both and advances each head once`, `an external notify advances the stamp on every call; a push publishing an unchanged record does not`, `concurrent notifies of one record receive distinct stamps`, `rejected mutation publishes nothing even though it called publish first`, `live transport negotiates, wakes only after commit…`; sim `d3_one_change_is_one_stamp_on_every_channel` and the `republication cannot advance a stamp` invariant | covered | none; external writes are covered by the `backend.transaction` tests in [Persistence](../integration/persistence.md). |
| HTTP status mapping and limits ([Server transport](../../architecture/server/connection/transport.md)) | `HTTP adapter authenticates…` (401, 200, 400), `onError captures server-side failures…` (500), `listen answers pull…`, `HTTP maps engine codes to statuses: 403, 409 gap/overlap/version fields, 404, 405, 413, 400`, `HTTP classifies native failures by code, not message wording; unknown codes fall back to 500` | covered | Verified 2026-09-14 by `bash integration/persistence/server/run.sh`. |
| Live controller: one subscribe frame, wake after commit, page split, reconnect, duplicate receipt wakes nobody | `live transport negotiates, wakes only after commit, reconnects, and cleans up`; Rust `live_subscribe_…`, `live_page_progression_…`, `live_negotiation_…` | covered | A drain error closing with `1011` (and a failed negotiation closing with `1002`) is asserted against a fake native in `HTTP classifies native failures by code…`; upgrade refusals are in [Connection](../integration/connection.md). |
| Live controller transitions ([Controller §6](../../architecture/server/connection/controller.md)): listeners before the acknowledgement and one pull per channel from its head; one extra pull for any number of commits during a pull; unchanged pages are not sent; a full page continues, a short page ends the drain; nothing after close; invalid page, unknown channel or unrequested page are errors; events and actions are tagged JSON | [live.rs](../../../../crates/server/tests/live.rs) (nine tests over `Subscriptions` with no host) | covered | Verified 2026-09-15 by `cargo test -p axton-server --locked`. The executor side (a commit between negotiation and the acknowledgement reaching the socket) is in [Connection](../integration/connection.md). |
