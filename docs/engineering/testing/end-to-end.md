# End-to-end tests

Verify complete application paths through a generated client, the real backend, PostgreSQL and local SQLite. Use a small number of representative flows that can detect missing wiring or incompatible assumptions between components.

Entry points: [round-trip.test.mjs](../../../integration/e2e/round-trip.test.mjs) drives the [round-trip fixture](../../../integration/e2e/fixtures/round-trip/README.md) with TypeScript and Dart clients (local visibility, server responses, live reconnect); [todo.test.mjs](../../../integration/e2e/todo.test.mjs) drives the public [To-do example](../../../examples/todo/README.md) backend.
Cross-runtime parity: [parity.test.mjs](../../../integration/e2e/parity.test.mjs), which runs one script through the Node client and the Dart client ([parity_client.dart](../../../integration/e2e/parity_client.dart)) against the same server and requires identical local state.
Subscription lifetime: [subscriptions.test.mjs](../../../integration/e2e/subscriptions.test.mjs), one assembled scenario for a subscription's origin, its persistence and its recovery (guarantee D9).
Whole-Scope loading: [bootstrap.test.mjs](../../../integration/e2e/bootstrap.test.mjs), the assembled #150 + #151 scenario set for the historical interval, its completion barrier and its failures (guarantee D10).

Because a subscription starts at the position the server acknowledges for it and loads nothing older ([#150](https://github.com/zanminwang/axton/issues/150)), every fixture here registers its subscription and waits for the boundary its first handshake commits before it expects any data. After that a fixture has two honest ways to meet existing rows, and which one it uses is part of what it tests: `subscription.bootstrap()`, the explicit historical load an application calls ([#151](https://github.com/zanminwang/axton/issues/151)), or touching the rows again, which is what a backend job does: a new stamp and a new position on every Channel they belong to. The To-do scenarios and the bootstrap scenarios use the first; the Dart clients print `READY` and their harnesses touch the rows again, because those runs are about live delivery and the raw wire. No fixture substitutes cursor zero for a missing boundary.

After installing the prerequisites in [Running tests](running.md):

```sh
bash integration/e2e/run.sh
bash integration/e2e/todo-run.sh
```

`run.sh` runs the round-trip, subscription, bootstrap and parity tests; `todo-run.sh` runs the To-do scenarios.

Each runner builds native artifacts, generates its fixture or example APIs and starts a temporary PostgreSQL cluster. Assert user-visible state through the client, including rejected writes and resumed sync.

Next review: identify which full paths need a release gate and which cases are better isolated in component or integration tests. Extend the parity script only when a runtime-specific behavior (value conversion, error surfacing, session logic) is at stake; engine rules belong in the Rust suites. Device startup is separately exercised by [platform smoke tests](../../../integration/platform/README.md).

## Coverage review

Reviewed 2026-09-14, To-do row added 2026-09-15, subscription row added and the fixtures' setup reworded 2026-09-25, bootstrap row added and the To-do row reworded the same day with `bash integration/e2e/run.sh` and `bash integration/e2e/todo-run.sh` executed then (round-trip 3, subscriptions 1, bootstrap 10, parity 1, To-do 18, all passed); the other rows' tests were read, not re-executed. Rows reworded 2026-09-15 for completion from receipts and `bash integration/e2e/run.sh` executed the same day (round-trip 3 tests, parity 1 test, all passed). The suite runs a real backend over a temporary PostgreSQL cluster with the Node client, the Dart client and the fixture CLI.

| Path | Test | What it establishes | Limits |
| --- | --- | --- | --- |
| Node client → HTTP → Rust backend → Prisma → SQLite, then Dart against the same server | [round-trip.test.mjs](../../../integration/e2e/round-trip.test.mjs) first test | initial sync, offline edit visible before sync, frozen batch across restart, lost receipt converges without re-executing, rejection reported, local writes not blocked by an in-flight push, background connection with pause and resume, Dart write completes from its receipt | The Node part drives the wire through the internal `syncProtocol` fixture for the first half and `connect` for the second; the Dart part writes a different record, so identical outcomes are not compared. |
| Fixture console client | second test | the subscription reports `ready/live` before any record arrives, then the offline edit stays local, syncs when online, and the normalized value comes back | Depends on the fixture's console output strings. |
| Built-in live sync in both languages | third test with [dart_live_client.dart](../../../integration/e2e/dart_live_client.dart) | multi-page catch-up (56 records), a commit during a held catch-up is not missed, watch fires, dependent pushes complete from their receipts and the streamed pages deliver the same authority without polling, offline reconnect resumes from the persisted cursor, Dart repeats the flow including unsubscribe and resubscribe (and that unsubscribing keeps the local records, guarantee D6) | Timing assertions use polling with fixed timeouts; a slow host can produce false failures rather than false passes. |
| To-do backend scenarios | [todo.test.mjs](../../../integration/e2e/todo.test.mjs) | seeds survive a restart, happy path convergence, 401 for an unknown identity, `todo.missing`, `todo.id_conflict` without overwrite, lost receipt runs the handler once, offline add-then-done across a reopen, opposing completions in both orders, an Action submitted while the historical page is held on the wire | Node clients only; the two-phone flow runs in the [To-do simulator smoke](../../../integration/platform/run_todo_ios_smoke.sh), outside the host gate. Each client meets the seeds through `subscription.bootstrap()`, as the example app does, once it reports its subscription initialized; a client whose token is refused never gets them. One test keeps `app.publishSeeds()`, because it is about a raw-wire pull from a committed cursor rather than about the load. |
| Operation results and Channel distribution ([#140](https://github.com/zanminwang/axton/issues/140)) | [action.test.mts](../../../integration/action-e2e/action.test.mts), run by `bash integration/action-e2e/run.sh` | on the direct and durable routes: an edit of Todo A that returns Todo B through a same-name output reconciles local A on completion, returns B's Loader snapshot, stamps A once with one position and B not at all, and `store: false` keeps B out of the local Model but never A's authority; a retried call replays its saved result with no new stamp or position; a no-output edit completes once local A holds its authority; a touch of a Note, from a caller whose request declares only Todo, commits and reaches another client subscribed to the Note's Channel; RetitleTodos' extra touches are not caller authority | The caller that lacks Note is a raw `/sync/actions` or `/sync/mutations` request, because a generated client always declares every Model of its schema. One disposable PostgreSQL cluster (14.x locally, 16 in CI). |
| One script, two runtimes: catch-up, an accepted edit, a rejected edit, a direct local create | [parity.test.mjs](../../../integration/e2e/parity.test.mjs) with [parity_client.dart](../../../integration/e2e/parity_client.dart) | before each runtime the server is reseeded (entry-1 back to its initial text, published on the channel), so both start from the same authoritative state; each runtime records the value it saw after catch-up and after the accepted edit, and those are asserted per runtime (seeded value, then the server-normalized `parity`) before the rejected edit; then the Node and Dart dumps (records, pending, before images, channels, rejections, per-record status of the edited and the local-only record) must be deep-equal, and the final outcomes are checked against the script | Cursors and client ids are excluded because two clients legitimately differ there. Both runtimes use one backend and one PostgreSQL database with a reseed between runs, not two isolated databases. Checked once by hand: a Dart run that kept the old authoritative text after completion fails the parity comparison. The Rust engine is shared, so this proves the host-side session logic and value handling agree, not the engine twice; a Rust-native third runner is not built. Verified 2026-09-15 by `bash integration/e2e/run.sh` with completion from receipts. |
| A subscription's origin, persistence and recovery | [subscriptions.test.mjs](../../../integration/e2e/subscriptions.test.mjs) | an offline registration commits with no boundary and one handle per Scope; the first handshake commits the acknowledged head *S* as both boundaries; records published before *S* are never loaded; a later publication arrives on the stream; a publication during a socket outage is filled by catch-up from the committed cursor with *S* unchanged; closing and reopening the local database keeps the identity and both boundaries, and the next session resumes from the cursor without initializing again | One Node client and one Scope; a crash boundary is not simulated (close and reopen only), and the Rust-level cases - head zero, a head below the cursor, a mixed Scope set, a rebuild - stay in [sqlite/tests/downlink_worker.rs](../../../crates/sqlite/tests/downlink_worker.rs). |
| Whole-Scope loading against a real backend | [bootstrap.test.mjs](../../../integration/e2e/bootstrap.test.mjs) | ten scenarios: a record removed and added back above the origin (a fresh position at its old stamp) while the first page is held on the wire is delivered by the subscription and not by the scan, and the compacted position makes the first page fifty rows across fifty-one positions; an older page resolved before a newer update and a newer deletion regresses neither; an exact fifty-record interval is one terminal page and a zero-head Scope completes; three bounded pages finish an interval under continuous publication and everything converges once writes stop; the barrier is the head the final page saw and stays fixed while the head moves, with the observed phase order `not-requested` → `loading` → `catching-up` → `complete`; two overlapping Scopes keep their own origins, progress and barriers; a queued local write settles mid-interval from its receipt; a load registered offline completes once connected; one interrupted by closing the client resumes without a new call; a live read failure stays reported when the load completes; and a failed historical page keeps its other records, holds the interval, rejects the call and is retried explicitly | One Node client per scenario against one PostgreSQL database, with each scenario resetting the invalidation log so its cursors are exact. The historical pages and the ordinary catch-up are held by intercepting `fetch`, which is how "before the scan reaches it" and "delivery has not reached the barrier" are made deterministic; the live WebSocket is not intercepted. Local PostgreSQL is 14.x and CI is 16. A crash boundary is not simulated (close and reopen only), and the Dart surface is covered in [dart/test/subscriptions_test.dart](../../../packages/dart/test/subscriptions_test.dart) rather than here. |
| iOS device startup | [platform smoke](../../../integration/platform/run_ios_simulator_smoke.sh) | the native library loads and the app starts on a simulator | Manual, outside the host gate. |

The suite is the only place the real TypeScript backend, the real PostgreSQL adapter and a real generated client meet. It should stay small; each of its assertions is also covered at a lower level except the wiring itself.
